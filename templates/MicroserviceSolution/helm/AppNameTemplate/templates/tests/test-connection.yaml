apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "AppNameTemplate.fullname" . }}-test-connection"
  labels:
{{ include "AppNameTemplate.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "AppNameTemplate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
