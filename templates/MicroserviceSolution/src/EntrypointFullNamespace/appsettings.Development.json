{
//#if (silverback)
    "Silverback": {
        "BootstrapServers": "PLAINTEXT://localhost:9092",
        "DefaultErrorPolicy": {
            "Retry": {
                "InitialDelay": "00:00:00.1",
                "MaxFailedAttempts": 3
            }
        },
        "CatchAllErrorPolicy": {
            "Hospitalize": {
                "InitialDelay": "00:00:10",
                "MaxRetries": 3
            }
        }
    },
//#endif
"ConnectionStrings": {
//#if (mongodb)
    "MongoDb": "mongodb://localhost:27017/database-name",
//#endif
//#if (redis)
    "Redis": "localhost,ssl=False,abortConnect=False,defaultDatabase=0,instanceName=AppNameTemplate,ActiveTracer=false"
//#endif
  }
}